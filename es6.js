// template string
const numbers = [45, 52, 48, 52];

const student = {
    name: 'salam',
    age: 45,
    movies: ['king', 'badsha']
};
const about = `My name is ${student.name} age of ${student.age} also liked movie 
${student.movies[1]}`
console.log(about);

// arrow function
